/**
 * @param {number} N
 * @return {boolean}
 */
// N - X === 3 ： false
// N - X === 2 ： true
/**
 * @param {number} N
 * @return {boolean}
 */
var divisorGame = function(N) {
    if(N % 2 === 0) return true;
    return false;
};